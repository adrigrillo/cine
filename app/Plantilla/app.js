(function() {
'use strict';

// Declare app level module which depends on views, and components
var app = angular.module('cine', ['ngRoute'])

.config(['$routeProvider', function($routeProvider) {

}]);
})
